# Java / Maven
backend/target/
*.class
*.jar
!**/maven-wrapper.jar

# Node / Next.js
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/.env*.local

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store

# Local secrets
.env

# Credentials for one-off scripts (scripts/seed-remote.sh). Kept in a file
# rather than on the command line so they never reach shell history, `ps`
# output, or a screenshot of a terminal.
backend/.env.seed
*.env.seed
!*.env.seed.example
